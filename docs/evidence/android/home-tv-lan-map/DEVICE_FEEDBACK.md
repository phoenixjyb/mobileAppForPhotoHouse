# JMGO user feedback — connection and synthetic image observed

After the private v3 APK handoff, the user reported that the connection went
through on the projector and the synthetic color-stripe picture was visible.
This is user-observed physical connection/rendering evidence. The installed
package/version/signature was not independently read back through ADB, so the
expected v3 APK identity remains the artifact in RETURN.md rather than a new
verified installed-binary identity.

The user next requested serving their real album. Backend owns real album/selection
discovery and preparation of the reviewed normalized grid/display publication.
The exact real album or initial photo selection is pending the user's choice;
no entire-library exposure is inferred. Android's existing frozen feed adapter
needs no source change solely to display the selected real-photo derivatives.

Remote/slideshow/sleep-wake, long-running reliability, native projection resolution
and real-photo acceptance remain separate from this reported connection success.
